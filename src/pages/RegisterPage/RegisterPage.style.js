import {makeStyles} from "@mui/styles";

export const useStyle = makeStyles({
    registerSection: {
        minHeight: "90vh", display: "flex", justifyContent: "center", alignItems: "center",
    }, siteLogo: {
        margin: "0 auto", marginBottom: "1.75rem", width: "3.87rem", height: "3.87rem"
    }
})