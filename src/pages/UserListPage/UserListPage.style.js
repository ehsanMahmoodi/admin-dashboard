import {makeStyles} from "@mui/styles";

export const useStyle = makeStyles({
    sectionTitle: {
        margin: '2rem 0 !important', textTransform: 'capitalize'
    }, userSection: {
        boxShadow: "0 0 6px #cbcbcb",
        background: '#fff',
        padding: '1.27rem',
        borderRadius: '0.62rem',
        marginBottom: '1rem'
    }
})